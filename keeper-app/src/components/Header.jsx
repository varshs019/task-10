import React from 'react';

const Header = () => (
  <nav className="navbar navbar-dark bg-primary mb-4">
    <div className="container-fluid">
      <span className="navbar-brand mb-0 h1">Keeper App</span>
    </div>
  </nav>
);

export default Header; 